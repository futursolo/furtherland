---
title: 'Test Article'
date: '2099-12-31'
slug: 'test'
---

import BarChart from '@@post-components/shared/BarChart';
import RadarChart from '@@post-components/shared/RadarChart';

Hello world!

This is some Rust Code.

```rust
let s = "String".to_string();
```

This is some Python Code.

```python
s = [i + 1 async for i in a if i > 0]
```

This is a test table.

| Language | Created | Status |
| --- | --- | --- |
| Rust | 1991 | Stable |
| Python | 1991 | Stable |
| JavaScript | 1995 | Stable |
| TypeScript | 2012 | Stable |

This is a test BarChart.

<BarChart client:only="react"
  xAxis={[{ data: ['group A', 'group B', 'group C'] }]}
  series={[{ data: [4, 3, 5] }, { data: [1, 6, 3] }, { data: [2, 5, 6] }]}
  height={300}
/>

This is a test RadarChart.

<RadarChart client:only="react"
  series={[{ data: [3, 2, 2, 5], label: 'A' }, { data: [4, 3, 5, 4], label: 'B' }]}
  radar={{ metrics: ['Metric A', 'Metric B', 'Metric C', 'Metric D'] }}
  height={300}
/>
