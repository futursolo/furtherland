import { styled } from '@@frontend/utils';

const H2 = styled('h2')({
  display: 'flex',
  width: '100%',

  flexDirection: 'column',
  alignItems: 'flex-start',
  justifyContent: 'flex-start',

  fontSize: '2rem',
});

export default H2;
